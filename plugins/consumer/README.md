# consumer

Buying-side decision support for durable goods — printers, machines, cameras, bikes, instruments,
tools, vehicles.

Run `/consumer:introduce` for the full capability list. That skill is the authoritative reference.
